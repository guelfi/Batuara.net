using System;

namespace Batuara.Domain.Enums
{
    public enum ContactMessageStatus
    {
        New = 1,
        InProgress = 2,
        Resolved = 3,
        Archived = 4
    }

    public enum ContributionPaymentStatus
    {
        Pending = 1,
        Paid = 2
    }
}
